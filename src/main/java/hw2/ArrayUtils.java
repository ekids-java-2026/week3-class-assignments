package hw2;

/**
 * ======================================================
 *   დავალება 2 - საშუალო: ArrayUtils (სტატიკური მეთოდები)
 * ======================================================
 *
 * შენი დავალება: დაწერე ქვემოთ მოცემული თითოეული სტატიკური მეთოდი.
 * თითოეული მეთოდი მუშაობს int[] მასივთან.
 *
 * მასივების შეხსენება:
 *   int[] arr = {3, 7, 2, 9};
 *   arr.length  → 4          (ელემენტების რაოდენობა)
 *   arr[0]      → 3          (პირველი ელემენტი)
 *   arr[3]      → 9          (ბოლო ელემენტი)
 *
 * ახალი მასივის შექმნა:
 *   int[] result = new int[arr.length];
 *
 * გაუშვი ArrayUtilsTest შენი პასუხების შესამოწმებლად.
 * მწვანე = სწორია,  წითელი = გამოსასწორებელია.
 */
public class ArrayUtils {

    /**
     * აბრუნებს მასივის ყველა ელემენტის ჯამს.
     * შეგიძლია ვივარაუდოთ, რომ მასივი ცარიელი არ არის.
     * მაგალითი: sum(new int[]{3, 7, 2, 9}) → 21
     *           sum(new int[]{10, -3, 5})  → 12
     */
    public static int sum(int[] arr) {
        // TODO: დაწერე შენი კოდი აქ
        // მინიშნება: შექმენი ჯამის ცვლადი 0-ით, შემდეგ გაიარე მასივი for ციკლით
        return 0;
    }

    /**
     * აბრუნებს true-ს თუ target მასივში გვხვდება, false-ს თუ არ გვხვდება.
     * მაგალითი: contains(new int[]{3, 7, 2, 9}, 7)  → true
     *           contains(new int[]{3, 7, 2, 9}, 5)  → false
     */
    public static boolean contains(int[] arr, int target) {
        // TODO: დაწერე შენი კოდი აქ
        // მინიშნება: ციკლში თუ arr[i] == target, დაუბრუნე true დაუყოვნებლივ
        return false;
    }

    /**
     * ითვლის და აბრუნებს ლუწი რიცხვების რაოდენობას მასივში.
     * მაგალითი: countEvens(new int[]{1, 2, 3, 4, 6}) → 3
     *           countEvens(new int[]{1, 3, 5})        → 0
     */
    public static int countEvens(int[] arr) {
        // TODO: დაწერე შენი კოდი აქ
        // მინიშნება: გამოიყენე % ოპერატორი - ლუწი რიცხვია თუ arr[i] % 2 == 0
        return 0;
    }

    /**
     * აბრუნებს ახალ მასივს, სადაც ელემენტები მოცემული მასივის
     * საპირისპირო თანმიმდევრობით არის განლაგებული.
     * მაგალითი: reversed(new int[]{1, 2, 3, 4}) → {4, 3, 2, 1}
     *           reversed(new int[]{5, 10})       → {10, 5}
     */
    public static int[] reversed(int[] arr) {
        // TODO: დაწერე შენი კოდი აქ
        // მინიშნება: შექმენი ახალი მასივი: int[] result = new int[arr.length];
        //            შეავსე ის: result[i] = arr[arr.length - 1 - i]
        return new int[0];
    }

    /**
     * აბრუნებს true-ს თუ მასივის ყველა ელემენტი დადებითია (> 0),
     * false-ს თუ თუნდაც ერთი ელემენტი <= 0.
     * მაგალითი: areAllPositive(new int[]{3, 7, 2, 9})  → true
     *           areAllPositive(new int[]{3, -1, 2})    → false
     *           areAllPositive(new int[]{1, 0, 5})     → false  (0 არ არის დადებითი!)
     */
    public static boolean areAllPositive(int[] arr) {
        // TODO: დაწერე შენი კოდი აქ
        // მინიშნება: ციკლში თუ arr[i] <= 0 იპოვი, დაუბრუნე false დაუყოვნებლივ
        return false;
    }
}
