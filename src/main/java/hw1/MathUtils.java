package hw1;

/**
 * ======================================================
 *   დავალება 1 - მარტივი: MathUtils (სტატიკური მეთოდები)
 * ======================================================
 *
 * შენი დავალება: დაწერე ქვემოთ მოცემული თითოეული სტატიკური მეთოდი.
 * არ შეცვალო მეთოდების სახელები ან return ტიპები.
 * შეცვალე ყველა "return 0;" ან "return false;" შენი რეალური კოდით.
 *
 * გაუშვი MathUtilsTest შენი პასუხების შესამოწმებლად.
 * მწვანე = სწორია,  წითელი = გამოსასწორებელია.
 */
public class MathUtils {

    /**
     * აბრუნებს ორი მთელი რიცხვის ჯამს.
     * მაგალითი: add(3, 5) → 8
     */
    public static int add(int a, int b) {
        // TODO: დაწერე შენი კოდი აქ
        return 0;
    }

    /**
     * აბრუნებს ორი მთელი რიცხვის ნამრავლს (გამრავლება).
     * მაგალითი: multiply(4, 6) → 24
     */
    public static int multiply(int a, int b) {
        // TODO: დაწერე შენი კოდი აქ
        return 0;
    }

    /**
     * აბრუნებს true-ს თუ რიცხვი ლუწია, false-ს თუ კენტია.
     * მაგალითი: isEven(4) → true,   isEven(7) → false
     */
    public static boolean isEven(int n) {
        // TODO: დაწერე შენი კოდი აქ
        return false;
    }

    /**
     * აბრუნებს რიცხვის კვადრატს (n * n).
     * მაგალითი: square(5) → 25
     */
    public static int square(int n) {
        // TODO: დაწერე შენი კოდი აქ
        return 0;
    }

    /**
     * აბრუნებს ორი მთელი რიცხვიდან უფრო დიდს.
     * თუ ისინი ტოლია, დააბრუნე ნებისმიერი.
     * მაგალითი: max(10, 3) → 10,   max(7, 7) → 7
     */
    public static int max(int a, int b) {
        // TODO: დაწერე შენი კოდი აქ
        return 0;
    }

    /**
     * აბრუნებს n-ის აბსოლუტურ მნიშვნელობას (ყოველთვის არაუარყოფითი).
     * მაგალითი: abs(-5) → 5,   abs(3) → 3,   abs(0) → 0
     */
    public static int abs(int n) {
        // TODO: დაწერე შენი კოდი აქ
        return 0;
    }
}
