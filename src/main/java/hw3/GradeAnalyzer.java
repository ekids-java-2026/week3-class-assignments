package hw3;

/**
 * ======================================================
 *   დავალება 3 - რთული: GradeAnalyzer (სტატიკური მეთოდები)
 * ======================================================
 *
 * წარმოიდგინე, რომ ქმნი სკოლის შეფასების სისტემას.
 * თითოეულ მოსწავლეს აქვს ქულა 0-დან 100-მდე.
 * უნდა დაწერო სტატიკური მეთოდები ამ ქულების გასაანალიზებლად.
 *
 * ამ სკოლის შეფასების სისტემა:
 *   90 - 100  →  "A"  (შესანიშნავი)
 *   75 -  89  →  "B"  (კარგი)
 *   60 -  74  →  "C"  (საშუალო)
 *   40 -  59  →  "D"  (საშუალოზე დაბალი)
 *    0 -  39  →  "F"  (ჩაიჭრა)
 *
 * მოსწავლე ჩაბარებულად ითვლება თუ ქულა >= 40.
 *
 * ეს დავალება იყენებს int მასივებს. გახსოვდეს:
 *   int[] scores = {85, 92, 67};
 *   scores.length   → 3
 *   scores[0]       → 85
 *
 * გაუშვი GradeAnalyzerTest შენი პასუხების შესამოწმებლად.
 * მწვანე = სწორია,  წითელი = გამოსასწორებელია.
 */
public class GradeAnalyzer {

    /**
     * აბრუნებს შეფასების ასოს ერთი ქულისთვის.
     * გამოიყენე ზემოთ მოცემული ცხრილი.
     * მაგალითი: getLetterGrade(95) → "A"
     *           getLetterGrade(72) → "C"
     *           getLetterGrade(30) → "F"
     */
    public static String getLetterGrade(int score) {
        // TODO: დაწერე შენი კოდი აქ
        // მინიშნება: გამოიყენე if / else if / else
        return "";
    }

    /**
     * აბრუნებს ყველა ქულის საშუალო მნიშვნელობას, დამრგვალებულს ქვევით (int).
     * შეგიძლია ვივარაუდოთ, რომ მასივს აქვს მინიმუმ ერთი ელემენტი.
     * მაგალითი: average(new int[]{80, 90, 70}) → 80
     *           average(new int[]{55, 65})      → 60
     */
    public static int average(int[] scores) {
        // TODO: დაწერე შენი კოდი აქ
        return 0;
    }

    /**
     * აბრუნებს მასივში ყველაზე მაღალ ქულას.
     * შეგიძლია ვივარაუდოთ, რომ მასივს აქვს მინიმუმ ერთი ელემენტი.
     * მაგალითი: findMax(new int[]{70, 95, 60, 88}) → 95
     */
    public static int findMax(int[] scores) {
        // TODO: დაწერე შენი კოდი აქ
        // მინიშნება: დაიწყე scores[0]-ით როგორც მაქსიმუმით, შემდეგ გაიარე ციკლით
        return 0;
    }

    /**
     * აბრუნებს მასივში ყველაზე დაბალ ქულას.
     * შეგიძლია ვივარაუდოთ, რომ მასივს აქვს მინიმუმ ერთი ელემენტი.
     * მაგალითი: findMin(new int[]{70, 95, 60, 88}) → 60
     */
    public static int findMin(int[] scores) {
        // TODO: დაწერე შენი კოდი აქ
        return 0;
    }

    /**
     * ითვლის და აბრუნებს რამდენმა მოსწავლემ ჩააბარა (ქულა >= 40).
     * მაგალითი: countPassed(new int[]{85, 30, 60, 20, 75}) → 3
     */
    public static int countPassed(int[] scores) {
        // TODO: დაწერე შენი კოდი აქ
        return 0;
    }

    /**
     * აბრუნებს true-ს თუ მასივის ყველა მოსწავლემ ჩააბარა (ქულა >= 40),
     * false-ს თუ თუნდაც ერთმა ვერ ჩააბარა.
     * მაგალითი: allPassed(new int[]{50, 60, 70}) → true
     *           allPassed(new int[]{50, 35, 70}) → false
     */
    public static boolean allPassed(int[] scores) {
        // TODO: დაწერე შენი კოდი აქ
        // მინიშნება: ციკლში თუ ერთ ვერ ჩამბარებელს იპოვი, დაუბრუნე false დაუყოვნებლივ
        return false;
    }

    /**
     * ბონუსი (დამატებითი გამოწვევა): აბრუნებს შეჯამების სტრინგს ამ ფორმატში:
     *   "Max: X | Min: Y | Avg: Z | Passed: P/N"
     * სადაც N არის მოსწავლეთა საერთო რაოდენობა.
     *
     * მაგალითი: {85, 30, 60, 20, 75} ქულებისთვის შედეგია:
     *   "Max: 85 | Min: 20 | Avg: 54 | Passed: 3/5"
     *
     * მინიშნება: გამოიყენე მეთოდები, რომლებიც ზემოთ უკვე დაწერე!
     */
    public static String summary(int[] scores) {
        // TODO: დაწერე შენი კოდი აქ
        // მინიშნება: გამოიყენე findMax(), findMin(), average(), countPassed(), scores.length
        return "";
    }
}
